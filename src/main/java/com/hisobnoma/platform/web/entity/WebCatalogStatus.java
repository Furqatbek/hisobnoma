package com.hisobnoma.platform.web.entity;

/**
 * Publication status of a web catalog item.
 * DRAFT items are only visible to staff; LIVE items are visible in the online shop.
 */
public enum WebCatalogStatus {
    DRAFT,
    LIVE
}
